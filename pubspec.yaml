name: khalti_client
description: A plugin for implementing khalti client api in flutter.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  plugin:
    platforms:
      android:
        package: com.sanzaaltech.khalti_client
        pluginClass: KhaltiClientPlugin
      ios:
        pluginClass: KhaltiClientPlugin

